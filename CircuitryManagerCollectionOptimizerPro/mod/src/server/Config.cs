namespace GHXX_CircuitryManagerCollectionOptimizerPro.Server
{
    static class Config
    {
        /// <summary>
        /// The initial hashset size
        /// </summary>
        public const int InitialHashsetSizePrime = 3; // initial hashset size, typically this is 3 by default. Setting it to a larger value may increase memory usage, but also increase execution speed
    }
}
